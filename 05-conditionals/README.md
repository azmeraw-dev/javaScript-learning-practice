📘 JavaScript Conditionals — Key Concepts

🔹 1. if Statement
- Checks if a condition is true and runs code inside the block.
Example: 
if (age >= 18) { console.log("Adult"); }

🔹 2. if...else Statement
- Adds an alternative path if the condition is false.
Example:
if (age >= 18) { ... } else { ... }

🔹 3. if...else if...else Ladder
- Used when you have multiple conditions to test.
Example:
if (score >= 90) {...} else if (score >= 75) {...} else {...}

🔹 4. Ternary Operator
- Short form of if...else in a single line.
Syntax: condition ? valueIfTrue : valueIfFalse

🔹 5. switch Statement
- Used when you have many possible values for a single variable.
Example:
switch(day) { case "Monday": ... break; default: ... }

✅ Tips
- Always use === for strict equality.
- Don’t forget 'break' in switch cases.
- Use logical operators (&&, ||, !) for combining conditions.
