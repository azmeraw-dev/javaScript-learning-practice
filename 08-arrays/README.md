# 📘 JavaScript Arrays

- Arrays store multiple values in a single variable.
- Elements are accessed by index (starting from 0).
- Arrays can be modified using methods like:
  - `push()`, `pop()`, `shift()`, `unshift()`
  - `includes()`, `length`
- You can loop through arrays using `for` or `for...of`.

## 🧩 Practice Ideas
1. Favorite foods list  
2. Shopping cart calculator  
3. To-do list manager  
